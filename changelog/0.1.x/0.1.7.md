---
summary: "Code simplification, error code corrections (InvalidParams → ValidationError), mcp-ts-core ^0.9.7 → ^0.9.9, skills synced"
breaking: false
security: false
---

# 0.1.7 — 2026-05-24

## Changed

- **`mcp-ts-core`** ^0.9.7 → ^0.9.9 (`@hono/node-server` ^2.0.3 → ^2.0.4; fuzz pre-parse fix; `fast-check` moved to optional peer dep; `git-wrapup` skill added; "Close the loop on issues" rule in CLAUDE.md/AGENTS.md).
- **Error codes** — 4 sites corrected from `InvalidParams` to `ValidationError`: `invalid_dimension` and `conflicting_params` (×2) error contracts in `eurostat-query-dataset.tool.ts`, and the generic HTTP 400 branch in `eurostat-data-service.ts`. Imports updated: `invalidParams` → `validationError`.
- **`indexesWhere` helper** — consolidates 3 repeated `entries.map((_, i) => i).filter(...)` patterns in `eurostat-catalogue-service.ts` into a single private method.
- **Code simplification** — `Number.isNaN` over global `isNaN` (2 sites); dot notation over bracket access (4 sites); `?.trim() || undefined` optional-chaining form for `themeCode`; removed spurious `async` on resource handler; `biome-ignore` suppression replaces stale `eslint-disable` comments; destructure-after-length-guard replaces repeated `cols[N]!` non-null assertions in TOC parser.
- **Skills** — `git-wrapup` v1.0 added; `release-and-publish` v2.5 → v2.6.
- **CLAUDE.md / AGENTS.md** — "Close the loop on issues" rule; framework version reference updated.
